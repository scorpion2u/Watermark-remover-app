name: Android Minimal Check

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  minimal:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: List files
        run: |
          echo "Listing repo:"
          ls -la
